<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.acmed.his.dao.InspectDayMapper">

    <insert id="staticInspect">
        insert INTO t_r_inspect_day(orgcode,category,num,fee,date)
        select #{orgCode} as orgcode,category,
        count(*) as num, sum(fee) as fee,
        DATE_FORMAT(NOW(),'%Y-%m-%d') as date
        from t_b_inspect where prescriptionid in (select id from t_b_prescription where orgcode = 1)
        GROUP BY category ASC
    </insert>


</mapper>