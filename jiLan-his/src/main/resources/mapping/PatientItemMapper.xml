<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.acmed.his.dao.PatientItemMapper">
    <select id="getByMohu" resultType="com.acmed.his.model.PatientItem">
        SELECT * FROM t_b_patient_item
        WHERE orgcode = #{orgCode}
        <if test="@Ognl@isNotEmpty(vague)">
            AND (mobile like CONCAT(#{vague},'%') OR patientName LIKE CONCAT(#{vague},'%'))
        </if>
        <if test="@Ognl@isNotEmpty(blackFlag)">
            AND blackflag = #{blackFlag}
        </if>
    </select>
</mapper>